# ha-road-speed-limits

This project will serve to make a "Home Assistant" HACS integration that aggregates free speed limit information for display.

It will be saved to a GitHub repo named `ha-road-speed-limits`.
